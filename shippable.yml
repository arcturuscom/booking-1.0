language: java

jdk:

   - oraclejdk8

build:
  ci:
    - mvn install 
    - mvn test
   
env:
  - XUNIT_FILE=shippable/testresults/result.xml

after_success:
   - mvn clean cobertura:cobertura
   - mvn test

notifications:
  email:
      recipients:
         - benyazidanass@gmail.com
         - exampletwo@org.com